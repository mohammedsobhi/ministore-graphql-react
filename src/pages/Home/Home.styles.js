import styled from "styled-components";

export const HomeTitle = styled.h2`
  font-size: 1.5rem;
  font-size: clamp(1.5rem, 1.2rem + 1.5vw, 3rem);
  font-weight: 400;
  margin: 80px 0;
  margin: clamp(1.5rem, 0.7rem + 4vw, 5.5rem) 0;
`;
